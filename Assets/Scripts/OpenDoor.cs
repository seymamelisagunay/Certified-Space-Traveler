using System;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using DG.Tweening;

public class OpenDoor : MonoBehaviour
{
   /*public GameObject target;
   private Vector3 _firstPos;
   private void Start()
   {
      _firstPos = GetComponent<Transform>().localPosition;
      
   }

   private void OnTriggerEnter(Collider other)
   {
      Debug.Log("hebeleeee");
         gameObject.transform.DOLocalRotate(target.transform.forward , 2f);
   }

   private void OnTriggerExit(Collider other)
   {
      gameObject.transform.DOLocalRotate(_firstPos, 2f);
   }*/
}
